declare module '@revenuecat/purchases-capacitor' {
  // Runtime object
  export const Purchases: any;

  // Enums / constants
  export const LOG_LEVEL: any;

  // Types we actually reference in the codebase. You can flesh these out later or
  // install the official types, but `any` is enough to satisfy the compiler.
  export type PurchasesPackage = any;
  export type PurchasesOffering = any;
} 